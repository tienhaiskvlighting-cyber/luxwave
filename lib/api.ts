// API - Mock Data Only (no Directus needed for Phase 2 launch)

export { 
  getProducts,
  getProductBySlug,
  getCategories,
  getProjects,
  getProjectBySlug
} from './mock-data';

export type { Product, Category, Project } from './mock-data';
